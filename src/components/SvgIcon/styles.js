import { css } from 'glamor';

const styles = {
  icon: css({
    width: '1em',
    height: '1em',
    fill: 'currentColor',
    display: 'inline-block',
    verticalAlign: 'middle',
  }),
};

export default styles;
