import { css } from 'glamor';

const styles = {
  root: css({
    paddingTop: '0.25em',
  }),
  
  nav: css({
    padding: '0.25em',
    background: '#333',
  }),
  
  tiles: css({
    padding: '0.25em',
    margin: '0.25em',
    background: '#333',
    overflowX: 'hidden',
  }),
};

export default styles;