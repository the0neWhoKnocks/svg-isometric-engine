import { NAME } from './constants';

const FETCH_PROJECT = `${ NAME }/FETCH_PROJECT`;
const SET_PROJECT = `${ NAME }/SET_PROJECT`;
const SET_PROJECTS = `${ NAME }/SET_PROJECTS`;

export {
  FETCH_PROJECT,
  SET_PROJECT,
  SET_PROJECTS,
};
